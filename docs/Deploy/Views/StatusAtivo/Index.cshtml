@model IEnumerable<_3M.Comodato.Front.Models.StatusAtivo>

@using GridMvc.Html

@{
    ViewBag.Title = "Status do Equipamento";
}

<i class="fas fa-cubes fa-3x fa-pull-left"></i>
<h3>Status do Equipamento</h3>

<p>
    <a class="btn btn-primary btn-sm" href="StatusAtivo/Incluir">Novo registro <i class="far fa-sticky-note"></i></a>
</p>

<div class="table table-hover">
    @Html.Grid(Model).Columns(columns =>
    {
        columns.Add(foo => foo.DS_STATUS_ATIVO).Titled("DESCRIÇÃO").Sortable(true).Filterable(true);
        columns.Add()
                        .Encoded(false)
                        .Sanitized(false)
                        .SetWidth(30)
                        .RenderValueAs(o => Html.ActionLink(" ", "Editar", new { idKey = HttpUtility.HtmlDecode(o.idKey.ToString()) }, new { @class = "fas fa-pencil-alt fa-lg" }));
        columns.Add()
                        .Encoded(false)
                        .Sanitized(false)
                        .SetWidth(30)
                        .RenderValueAs(o => Html.ActionLink(" ", "Excluir", new { idKey = HttpUtility.HtmlDecode(o.idKey.ToString()) }, new { @class = "fas fa-trash-alt fa-lg" }));
    }).WithPaging(10)
</div>



